import * as React from 'react';
import styled from 'styled-components';

const App = () => (
    <React.Fragment>
        <BodyContainerStyle />
    </React.Fragment>
);

const BodyContainerStyle = styled.div``;

export default App;
